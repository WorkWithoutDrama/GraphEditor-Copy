# Graph Editor - Запуск на Windows

## 📋 Предварительные требования

Перед запуском установите:

1. **Node.js** (версия 14 или выше)
   - Скачайте с: https://nodejs.org/
   - Установите, выбрав вариант "Add to PATH"

2. **Python** (версия 3.8 или выше)
   - Скачайте с: https://www.python.org/
   - Установите, обязательно отметьте "Add Python to PATH"

3. **Ollama** (опционально, для LLM функциональности)
   - Скачайте с: https://ollama.ai/
   - Установите, затем выполните в командной строке:
     ```cmd
     ollama pull llama3.2
     ```

## 🚀 Запуск

### Вариант 1: Использование командного файла (launch.bat)

1. Откройте папку с проектом
2. Дважды кликните на `launch.bat`
3. Следуйте инструкциям в открывшемся окне

### Вариант 2: Использование PowerShell (launch.ps1)

1. Откройте папку с проектом
2. Щелкните правой кнопкой на `launch.ps1` → "Запустить с PowerShell"
3. Если появится предупреждение о безопасности, нажмите "Разрешить"
4. Следуйте инструкциям в PowerShell

### Вариант 3: Ручной запуск в командной строке

```cmd
# Проверьте установку
node --version
python --version

# Запустите API сервер (в одном окне)
python api_main.py

# В другом окне запустите прокси
node proxy-server.js

# Откройте в браузере
start http://localhost:3000/proxy-index.html
```

## 📁 Файлы запуска для Windows

- **`launch.bat`** - Командный файл для классического CMD
- **`launch.ps1`** - PowerShell скрипт (рекомендуется для Windows 10/11)

## 🔧 Функционал скриптов запуска

Скрипты автоматически выполняют:

1. ✅ Проверку установки Node.js и Python
2. ✅ Проверку и запуск Ollama (если установлен)
3. ✅ Остановку старых процессов
4. ✅ Запуск AI API сервера
5. ✅ Запуск прокси-сервера
6. ✅ Открытие браузера с Graph Editor
7. ✅ Автоматическую очистку при завершении

## 🐛 Решение проблем

### Проблема: "Скрипт не запускается"
**Решение:** Запустите от имени администратора или проверьте политики выполнения:
```powershell
# Для PowerShell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Проблема: "Python не найден"
**Решение:** Проверьте, что Python добавлен в PATH:
```cmd
python --version
```
Если не работает, переустановите Python с опцией "Add to PATH"

### Проблема: "Порт уже используется"
**Решение:** Закройте предыдущие экземпляры или измените порты в настройках

### Проблема: "Не открывается браузер"
**Решение:** Вручную откройте `http://localhost:3000/proxy-index.html`

## ⚙️ Расширенные настройки

### Изменение портов
По умолчанию:
- API сервер: порт 5009
- Прокси сервер: порт 3000

Для изменения портов отредактируйте:
- `api_main.py` (строка с `PORT = ...`)
- `proxy-server.js` (строка с `const PORT = ...`)

### Использование альтернативных LLM
Если Ollama не установлен, можно использовать:
1. DeepSeek через веб-интерфейс
2. Локальные модели через API

## 🛑 Остановка системы

### Для `launch.bat`:
- Закройте все окна командной строки
- Или нажмите Ctrl+C в основном окне

### Для `launch.ps1`:
- Нажмите Ctrl+C в окне PowerShell
- Или закройте окно

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в файлах `api_startup.log` и `api.log`
2. Убедитесь, что все порты свободны
3. Перезапустите скрипт
4. Проверьте установку зависимостей

## 🔄 Сравнение с macOS/linux версией

| Функция | macOS/linux (`launch.command`) | Windows (`launch.bat`/`launch.ps1`) |
|---------|--------------------------------|-------------------------------------|
| Проверка Node.js | ✅ | ✅ |
| Проверка Python | ✅ | ✅ |
| Проверка Ollama | ✅ | ✅ |
| Автозапуск браузера | ✅ | ✅ |
| Логи в реальном времени | ✅ | ✅ (в отдельном окне) |
| Автоматическая очистка | ✅ | ✅ |
| Графический интерфейс | Терминал | CMD/PowerShell |

## 💡 Советы

1. **Рекомендуется использовать PowerShell** (`launch.ps1`) для лучшей совместимости
2. **Добавьте исключение в антивирус** для папки проекта
3. **Используйте административные права** при проблемах с портами
4. **Проверяйте логи** при возникновении ошибок