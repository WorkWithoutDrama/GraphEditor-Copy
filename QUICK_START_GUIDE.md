# 🚀 Быстрый старт Graph Editor

## Проверка перед запуском:

### 1. Убедитесь, что все файлы на месте:
```bash
./test_final_setup.sh
```

### 2. Если порт 3000 занят, освободите его:
```bash
# Остановите текущий прокси
pkill -f "node proxy-server"
pkill -f "node simple-proxy"

# Или завершите процесс вручную
lsof -i :3000
kill -9 <PID>
```

## Запуск системы:

### Способ 1: Использование launch.command (рекомендуется)
```bash
./launch.command
```
**Что делает:**
- Проверяет Node.js и Python
- Запускает API сервер (порт 5009)
- Запускает прокси сервер (порт 3000)
- Открывает браузер с Graph Editor
- Показывает статус всех компонентов

### Способ 2: Ручной запуск
```bash
# 1. Запустите API сервер (в отдельном терминале)
python api.py

# 2. Запустите прокси сервер (в отдельном терминале)
node proxy-server.js

# 3. Откройте в браузере
open http://localhost:3000/proxy-index.html
```

### Способ 3: Если Node.js не в PATH
```bash
# Попробуйте стандартные пути
/usr/local/bin/node proxy-server.js
# или
/opt/homebrew/bin/node proxy-server.js
```

## Проверка работы:

### 1. API сервер:
```bash
curl http://localhost:5009/api/health
```
**Ожидаемый ответ:** `{"status": "ok", "service": "System Model Generator API", "llm_provider": "ollama"}`

### 2. Прокси сервер:
```bash
curl http://localhost:3000/api/health
```
**Ожидаемый ответ:** Должен проксировать запрос к API

### 3. Интерфейс:
Откройте в браузере: `http://localhost:3000/proxy-index.html`

## Устранение проблем:

### ❌ "Node.js не найден"
```bash
# Установите Node.js: https://nodejs.org/
# Или добавьте в PATH
export PATH="/usr/local/bin:$PATH"
```

### ❌ "Порт 3000 занят"
```bash
# Найдите и завершите процесс
lsof -i :3000
kill -9 <PID>

# Или используйте другой порт
# В proxy-server.js измените const PROXY_PORT = 3000;
```

### ❌ "API не запускается"
```bash
# Проверьте Python
python --version

# Проверьте зависимости
pip install -r requirements.txt  # если есть
```

### ❌ "404 Not Found" в браузере
Прокси сервер должен обслуживать статические файлы. Убедитесь, что:
1. `proxy-server.js` запущен
2. Файлы `proxy-index.html`, `graph-manager.js`, `script.js` существуют

## Дополнительно:

### Тестовый интерфейс:
Откройте: `http://localhost:3000/test-fix.html` для проверки логики составных ID.

### Документация:
Подробная документация по исправлениям: [FINAL_IMPLEMENTATION_SUMMARY.md](FINAL_IMPLEMENTATION_SUMMARY.md)

### Исходный код:
Основные файлы:
- `api.py` - AI API сервер
- `proxy-server.js` - прокси сервер
- `graph-manager.js` - логика работы с графами
- `script.js` - визуализация графов
- `proxy-index.html` - основной интерфейс

---

**Всё готово к работе!** После запуска откройте Graph Editor, нажмите "Graph Manager" и начните создавать модели.