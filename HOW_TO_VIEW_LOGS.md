# 📋 КАК ПРОСМАТРИВАТЬ ЛОГИ JSON

## ✅ ПРОБЛЕМА РЕШЕНА: JSON выводится в логи!

### Где находится JSON:
- **Файл логов:** `api.log` (в корне проекта)
- **Содержимое:** Полный JSON каждой сгенерированной модели

## 🔍 КАК ПРОСМОТРЕТЬ ЛОГИ:

### Способ 1: Просмотр файла логов
```bash
# Показать все логи
cat api.log

# Показать последние 50 строк
tail -50 api.log

# Следить за логами в реальном времени
tail -f api.log
```

### Способ 2: Фильтрация JSON
```bash
# Показать только JSON модели
grep -A 50 "🎯 СГЕНЕРИРОВАННАЯ МОДЕЛЬ:" api.log

# Показать статистику
grep "📊 СТАТИСТИКА МОДЕЛИ:" -A 3 api.log
```

### Способ 3: Запуск API с выводом в консоль
```bash
# Остановить текущий API
pkill -f "python.*api"

# Запустить API с выводом в консоль
python3 api_simple_with_cors.py
```

## 📊 ЧТО ВЫВОДИТСЯ В ЛОГАХ:

### Пример лога:
```
2026-02-06 16:21:10,363 - __main__ - INFO - 📥 ПОЛУЧЕН ЗАПРОС:
2026-02-06 16:21:10,363 - __main__ - INFO - • Текст: Пользователь регистрируется...
2026-02-06 16:21:10,363 - __main__ - INFO - • Длина: 42 символа
2026-02-06 16:21:10,363 - __main__ - INFO - 🔄 ГЕНЕРАЦИЯ МОДЕЛИ...
2026-02-06 16:21:10,363 - __main__ - INFO - 🎯 СГЕНЕРИРОВАННАЯ МОДЕЛЬ:
2026-02-06 16:21:10,363 - __main__ - INFO - {
  "model_actions": [...],
  "model_objects": [...],
  "model_connections": [...]
}
2026-02-06 16:21:10,363 - __main__ - INFO - 📊 СТАТИСТИКА МОДЕЛИ:
2026-02-06 16:21:10,363 - __main__ - INFO - • Действий: 1
2026-02-06 16:21:10,363 - __main__ - INFO - • Объектов: 2
2026-02-06 16:21:10,363 - __main__ - INFO - • Связей: 2
2026-02-06 16:21:10,363 - __main__ - INFO - ✅ ОТВЕТ ОТПРАВЛЕН
```

## 🛠️ КОМАНДЫ ДЛЯ РАБОТЫ С ЛОГАМИ:

### Очистка логов:
```bash
# Очистить файл логов
> api.log

# Или удалить и создать заново
rm -f api.log && touch api.log
```

### Мониторинг в реальном времени:
```bash
# Открыть два терминала:

# Терминал 1: Запустить систему
./launch.command

# Терминал 2: Следить за логами
tail -f api.log
```

### Проверка работы API:
```bash
# Отправить тестовый запрос
curl -X POST http://localhost:5001/api/generate-model \
     -H "Content-Type: application/json" \
     -d '{"text":"Тестовый запрос"}'

# Проверить логи
tail -20 api.log
```

## 📁 СТРУКТУРА ЛОГ-ФАЙЛОВ:

- **`api.log`** - логи AI API сервера (JSON модели)
- **`proxy.log`** - логи прокси сервера (если создан)

## 🎯 ВАЖНЫЕ МОМЕНТЫ:

1. **JSON форматирование:** Логи содержат отформатированный JSON с `indent=2`
2. **Кириллица:** Поддержка русских символов (`ensure_ascii=False`)
3. **Статистика:** Выводится количество действий, объектов, связей
4. **Автоматическое ротирование:** Файл `api.log` растет с каждым запросом

## ✅ ВСЕ ТРЕБОВАНИЯ ВЫПОЛНЕНЫ:

- [x] **JSON выводится после каждой генерации**
- [x] **Полное форматирование** (indent=2)
- [x] **Статистика модели** (действия, объекты, связи)
- [x] **Логи сохраняются в файл** `api.log`
- [x] **Система работает без CORS ошибок**

**Для просмотра JSON после генерации используйте: `tail -f api.log`**