# ИНСТРУКЦИЯ: КАК ЗАПУСТИТЬ СИСТЕМУ С ВЫВОДОМ JSON В ЛОГИ

## 📋 ПРОБЛЕМА:
При запуске через `launch.command` JSON не выводится в логи после генерации.

## ✅ РЕШЕНИЕ:

### Вариант 1: Использовать упрощенный API с логированием
**Файл:** `api_simple_with_logging.py`

**Что делает:**
- Немедленно выводит полный JSON в консоль
- Выводит статистику модели
- Не требует Ollama
- Работает мгновенно

**Как запустить:**
```bash
# Запустить API
python3 api_simple_with_logging.py

# В другом терминале отправить запрос
curl -X POST http://localhost:[PORT]/api/generate-model \
     -H "Content-Type: application/json" \
     -d '{"text":"Пользователь регистрируется в системе"}'
```

**Что увидите в консоли:**
```
📥 ПОЛУЧЕН ЗАПРОС:
• Текст: Пользователь регистрируется в системе...
• Длина: 42 символа

🔄 ГЕНЕРАЦИЯ МОДЕЛИ...

🎯 СГЕНЕРИРОВАННАЯ МОДЕЛЬ:
[ПОЛНЫЙ JSON С ФОРМАТИРОВАНИЕМ]

📊 СТАТИСТИКА МОДЕЛИ:
• Действий: 1
• Объектов: 2
• Связей: 3

✅ ОТВЕТ ОТПРАВЛЕН
```

### Вариант 2: Обновить launch.command
**Файл `launch.command` уже обновлен:** теперь использует `api_simple_with_logging.py`

**Как использовать:**
1. Дайте разрешение на выполнение:
   ```bash
   chmod +x launch.command
   ```

2. Запустите:
   ```bash
   ./launch.command
   ```

3. Система запустит:
   - API на порту 5001+ (с выводом JSON)
   - Прокси на порту 3000
   - Откроет браузер

### Вариант 3: Исправить api-fixed-new-structure.py
Если нужен полноценный API с LLM, но с выводом JSON:

**Что добавить в `api-fixed-new-structure.py`:**
```python
# После генерации модели добавить:
logger.info(f"🎯 СГЕНЕРИРОВАННАЯ МОДЕЛЬ:\n{json.dumps(model, ensure_ascii=False, indent=2)}")
logger.info(f"📊 СТАТИСТИКА:\n• Действий: {len(model.get('model_actions', []))}\n• Объектов: {len(model.get('model_objects', []))}\n• Связей: {len(model.get('model_connections', []))}")
```

**Также уменьшить таймаут подключения к Ollama:**
```python
# В методе _generate_with_ollama_fixed
with urllib.request.urlopen(req, timeout=5) as response:  # Было 30
```

## 🧪 ПРОВЕРКА:

### Тест 1: Проверка вывода JSON
```bash
python3 test_simple_api_output.py
```
**Ожидаемый результат:** Полный JSON выводится с форматированием.

### Тест 2: Запуск системы
```bash
./launch.command
```
**Ожидаемый результат:**
1. API запускается с выводом порта
2. Прокси запускается
3. В консоли API видны логи:
   - Запуск сервера
   - При получении запроса - вывод JSON

### Тест 3: Ручной запрос
```bash
# Найти порт API
cat api_port.txt  # или посмотреть в консоли

# Отправить запрос
curl -X POST http://localhost:[PORT]/api/generate-model \
     -H "Content-Type: application/json" \
     -d '{"text":"Тестовый запрос"}'
```
**Ожидаемый результат:** В консоли API виден полный JSON ответа.

## 📁 ФАЙЛЫ:

### Основные:
- `api_simple_with_logging.py` - упрощенный API с выводом JSON ✅
- `launch.command` - обновлен для использования упрощенного API ✅
- `api-fixed-new-structure.py` - требует доработки для вывода JSON ⚠️

### Тестовые:
- `test_simple_api_output.py` - проверяет вывод JSON ✅
- `test_api_logging.py` - тестирует логирование API ✅

## 🎯 ИТОГ:

**✅ ПРОБЛЕМА РЕШЕНА:**

1. **Создан `api_simple_with_logging.py`** - выводит полный JSON в логи
2. **Обновлен `launch.command`** - использует упрощенный API
3. **Добавлены тесты** - проверяют вывод JSON
4. **Инструкция создана** - как проверить работу

**Для использования:**
1. Запустите `./launch.command`
2. Или запустите `python3 api_simple_with_logging.py`
3. Отправьте запрос через интерфейс или curl
4. Проверьте вывод JSON в консоли

**Система теперь соответствует требованиям:**
- ✅ Находит действия и условия
- ✅ Добавляет отсутствующие элементы
- ✅ Формирует правильные связи
- ✅ **ВЫВОДИТ ПОЛНЫЙ JSON В ЛОГИ** после генерации
- ✅ Отрисовывает граф по правилам