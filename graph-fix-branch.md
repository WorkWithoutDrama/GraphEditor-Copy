# Ветка: fix-graph-display-issues

## 🎯 Задача
Исправить проблемы с отображением графа:
1. **Формы одинакового размера** вне зависимости от содержания
2. **Текст не помещается** внутри форм
3. **Длинный текст не разбивается** на строки

## 📋 Проблемы в текущей реализации

### В файле `script.js`:
1. **Фиксированные размеры** для узлов:
   ```javascript
   'width': '80px',
   'height': '40px',
   ```
2. **Нет автоматического расчета** размера под текст
3. **Нет переноса текста** на несколько строк

## 🔧 План исправлений

### 1. **Динамический расчет размеров**
- Рассчитывать ширину и высоту на основе длины текста
- Использовать `label` и `text-wrap` свойства Cytoscape
- Добавить отступы в зависимости от текста

### 2. **Перенос текста**
- Использовать `text-wrap: wrap` для автоматического переноса
- Добавить `text-max-width` для ограничения ширины
- Настроить `line-height` для читаемости

### 3. **Разные стили для разных типов узлов**
- **Действия (action)**: прямоугольники с динамической шириной
- **Состояния (state)**: эллипсы с адаптивным размером

### 4. **Улучшение читаемости**
- Увеличить `font-size` для лучшей читаемости
- Добавить `padding` вокруг текста
- Использовать контрастные цвета

## 📁 Файлы для изменения

### Основные:
1. **`script.js`** - логика отображения графа (строки 11-170)
2. **Возможно `graph-manager.js`** - если нужно изменить данные узлов

### Вспомогательные:
1. **`styles.css`** - для дополнительных стилей если нужно
2. **Создать `graph-styles.js`** - если логика стилей станет сложной

## 💡 Предлагаемые изменения

### В `script.js`:
```javascript
// Текущий стиль (проблемный):
{ selector: 'node[type="action"]', style: {
    'shape': 'rectangle',
    'background-color': '#e6f7ff',
    'width': '100px',     // Фиксированная ширина
    'height': '50px'      // Фиксированная высота
} }

// Новый стиль (исправленный):
{ selector: 'node[type="action"]', style: {
    'shape': 'rectangle',
    'background-color': '#e6f7ff',
    'width': 'label',      // Ширина по тексту
    'height': 'label',     // Высота по тексту
    'text-wrap': 'wrap',   // Перенос текста
    'text-max-width': '150px', // Максимальная ширина
    'padding': '10px',     // Отступы
    'min-width': '60px',   // Минимальная ширина
    'min-height': '40px'   // Минимальная высота
} }
```

### Для эллипсов (состояния):
```javascript
{ selector: 'node[type="state"]', style: {
    'shape': 'ellipse',
    'background-color': '#f6ffed',
    'border-color': '#52c41a',
    'width': 'label',      // Адаптивная ширина
    'height': 'label',     // Адаптивная высота
    'text-wrap': 'wrap',
    'text-max-width': '120px',
    'padding': '12px',
    'min-width': '60px',
    'min-height': '60px'
} }
```

## 🧪 Тестирование

### Сценарии для проверки:
1. **Короткий текст** (1-3 слова) - должен отображаться компактно
2. **Средний текст** (5-10 слов) - должен переноситься на 2 строки
3. **Длинный текст** (15+ слов) - должен переноситься на несколько строк
4. **Очень длинный текст** - должен обрезаться или масштабироваться

### Типы узлов:
- **Действия**: "Пользователь регистрируется", "Система рассчитывает норму калорий"
- **Состояния**: "Пользователь зарегистрирован", "Базовая норма рассчитана"

## 🚀 Реализация

### Шаг 1: Базовые исправления
1. Заменить фиксированные размеры на `'width': 'label'`
2. Добавить `'text-wrap': 'wrap'`
3. Настроить `'text-max-width'`

### Шаг 2: Улучшение читаемости
1. Увеличить `font-size`
2. Добавить `padding`
3. Настроить контраст цветов

### Шаг 3: Оптимизация
1. Добавить `min-width` и `min-height`
2. Настроить `line-height`
3. Оптимизировать для разных типов узлов

### Шаг 4: Тестирование
1. Проверить все сценарии
2. Убедиться в читаемости
3. Проверить производительность

## 📈 Ожидаемые результаты

### После исправлений:
1. ✅ **Разные размеры** для разного текста
2. ✅ **Текст помещается** внутри форм
3. ✅ **Длинный текст разбивается** на строки
4. ✅ **Улучшенная читаемость**
5. ✅ **Эстетичный внешний вид**

### Критерии успеха:
- Все тексты читаемы без масштабирования
- Формы пропорциональны содержимому
- Нет наложений текста
- Приятный визуальный вид

## 🗓️ План работы

### День 1:
- Создать ветку `fix-graph-display`
- Внести базовые изменения в `script.js`
- Протестировать с короткими текстами

### День 2:
- Настроить перенос текста
- Оптимизировать для длинных текстов
- Провести comprehensive тестирование

### День 3:
- Улучшить стили для читаемости
- Оптимизировать производительность
- Создать документацию изменений

## 📚 Ресурсы

### Документация Cytoscape:
- [Node Styles](https://js.cytoscape.org/#style/node-body)
- [Label Styles](https://js.cytoscape.org/#style/labels)
- [Text Wrapping](https://js.cytoscape.org/#style/text-wrapping)

### Примеры кода:
```javascript
// Автоматический размер по тексту
'width': 'label',
'height': 'label',

// Перенос текста
'text-wrap': 'wrap',
'text-max-width': '150px',

// Отступы
'padding': '10px',
```

## 🎯 Цели качества

1. **Функциональность**: Все тексты отображаются корректно
2. **Читаемость**: Текст легко читается без усилий
3. **Эстетика**: Приятный и профессиональный вид
4. **Производительность**: Быстрое отображение даже с большими графами

**Ветка готова к работе!** 🚀