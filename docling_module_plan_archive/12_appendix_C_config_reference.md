# 12 — Appendix C: Configuration Reference (DoclingSettings)

Example (YAML/env-equivalent):

```yaml
DOCLING_ALLOWED_FORMATS: ["PDF","DOCX","PPTX","HTML","MARKDOWN","IMAGE"]
DOCLING_MAX_FILE_SIZE_BYTES: 20000000
DOCLING_MAX_NUM_PAGES: 200
DOCLING_PARSE_CONCURRENCY: 2
DOCLING_USE_PROCESS_POOL: true
DOCLING_PROCESS_POOL_WORKERS: 2

DOCLING_STORE_DOCLING_JSON: true
DOCLING_STORE_DOCLING_MD: false
DOCLING_ARTIFACT_TEXT_INLINE_MAX_CHARS: 200000

DOCLING_CHUNKER: "hybrid"
DOCLING_TARGET_TOKENS: 800
DOCLING_MAX_TOKENS: 1000
DOCLING_OVERLAP_TOKENS: 80
DOCLING_ENFORCE_MAX_TOKENS_HARD: true

DOCLING_DEDUPE_STRATEGY: "sha256"
DOCLING_REHYDRATE_FROM_ARTIFACT_JSON: true
```

Notes:
- Keep `MAX_TOKENS` below your embedding model limit with safety margin.
- If you see oversized chunks, keep hard enforcement enabled (Phase E).
