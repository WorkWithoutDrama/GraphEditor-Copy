# Быстрый старт с LLM интеграцией

## 🚀 Запуск системы (максимально просто)

### Для пользователей macOS:
1. **Дважды кликните** `launch.command`
2. **Разрешите выполнение**, если потребуется
3. **Дождитесь** запуска всех сервисов
4. **Откройте** Graph Editor в браузере

### Вручную через терминал:
```bash
# 1. Дайте права на выполнение
chmod +x launch.command cleanup.command

# 2. Запустите систему
./launch.command

# 3. Для остановки: Ctrl+C или закройте окно
# 4. Для очистки портов: ./cleanup.command
```

## 🤖 Настройка LLM (Ollama)

### Установка Ollama (если не установлен):
```bash
# Установите Ollama
brew install ollama  # macOS
# Или скачайте с https://ollama.ai/

# Загрузите модель
ollama pull llama3.2

# Проверьте установку
ollama --version
```

### Автоматическая настройка (через launch.command):
Скрипт `launch.command` автоматически:
1. Проверяет установку Ollama
2. Запускает сервер Ollama (если не запущен)
3. Проверяет наличие модели llama3.2
4. Загружает модель при необходимости

## 🌐 Использование LLM в интерфейсе

### Переключение провайдеров:
1. Откройте Graph Editor
2. Нажмите кнопку **"Graph Manager"** в редакторе
3. В появившемся чате найдите кнопку **"🤖 LLM"**
4. Нажмите для переключения:
   - **🤖 Ollama** - локальный LLM (рекомендуется)
   - **🤖 DeepSeek** - облачный LLM (требует API ключ)

### Генерация модели:
1. Введите текст ТЗ в поле ввода
2. Или загрузите файл через кнопку **"📎 Файл"**
3. Нажмите **"Отправить"**
4. Дождитесь генерации модели LLM
5. Модель отобразится в виде графа

## ⚙️ Конфигурация DeepSeek

### Если нужен DeepSeek вместо Ollama:
```bash
# 1. Получите API ключ на https://platform.deepseek.com/
# 2. Установите переменную окружения
export DEEPSEEK_API_KEY="ваш_ключ_здесь"

# 3. Или передайте ключ при запуске
DEEPSEEK_API_KEY="ваш_ключ" ./launch.command
```

### Переключение в интерфейсе:
1. Нажмите кнопку **"🤖 LLM"** для переключения на DeepSeek
2. Система автоматически использует ваш API ключ

## 🔧 Устранение неполадок

### Ollama не запускается:
```bash
# Проверьте установку
ollama --version

# Запустите вручную
ollama serve

# В другом терминале проверьте
curl http://localhost:11434/api/tags
```

### Модель не загружается:
```bash
# Загрузите вручную
ollama pull llama3.2

# Проверьте список моделей
ollama list
```

### API ключ DeepSeek не работает:
1. Проверьте баланс на https://platform.deepseek.com/
2. Убедитесь, что ключ установлен:
   ```bash
   echo $DEEPSEEK_API_KEY
   ```
3. Переустановите ключ:
   ```bash
   export DEEPSEEK_API_KEY="новый_ключ"
   ```

### LLM функции недоступны:
1. Система будет использовать **резервный режим**
2. Модели будут генерироваться на основе ключевых слов
3. Установите Ollama для полной функциональности

## 📊 Мониторинг системы

### Статус серверов:
После запуска `launch.command` отображается:
- ✅ **AI API**: http://localhost:[порт]/api/health
- ✅ **Прокси**: http://localhost:3000  
- ✅ **Редактор**: http://localhost:3000/proxy-index.html
- ✅/⚠️ **LLM**: Статус Ollama/DeepSeek

### Проверка вручную:
```bash
# Проверка AI API
curl http://localhost:5001/api/health

# Проверка прокси
curl http://localhost:3000/api/health

# Проверка Ollama
curl http://localhost:11434/api/tags
```

## 🛠️ Расширенные настройки

### Использование других моделей Ollama:
Отредактируйте `api.py`, строка ~50:
```python
"model": "llama3.2",  # Измените на нужную модель
```

### Доступные модели Ollama:
```bash
# Посмотреть все модели
ollama list

# Популярные модели:
ollama pull llama3.1  # Более новая версия
ollama pull mistral   # Другая архитектура
ollama pull gemma     # Легкая модель
```

### Кастомизация промпта:
Отредактируйте промпт в `api.py` (методы `_generate_with_ollama` и `_generate_with_deepseek`).

## 📈 Производительность

### Рекомендации:
- **Ollama**: 8GB+ RAM для llama3.2
- **DeepSeek**: Требует интернет-соединение
- **Резервный режим**: Работает на любом железе

### Оптимизация:
```bash
# Для слабых систем используйте легкие модели
ollama pull gemma:2b  # Легкая модель

# Или отключите LLM в интерфейсе
# Используйте только резервный режим
```

## 🆘 Поддержка

### Проблемы с запуском:
1. Запустите `cleanup.command` для очистки портов
2. Перезапустите `launch.command`
3. Проверьте логи в терминале

### Проблемы с LLM:
1. Убедитесь, что Ollama установлен и запущен
2. Проверьте наличие модели: `ollama list`
3. Перезапустите сервер: `pkill -f "ollama serve" && ollama serve`

### Другое:
- Проверьте наличие Node.js и Python 3
- Убедитесь, что порты 3000, 5001, 11434 свободны
- При проблемах с сетью используйте локальный Ollama