# Graph Editor - Исправленная версия

## 📋 Краткое описание

Проект Graph Editor с интегрированным AI API для генерации моделей систем. Все критические ошибки исправлены.

## 🐛 Исправленные проблемы

### **1. Ошибки создания ребер**
- **Было**: "Can not create edge with nonexistent target"
- **Исправлено**: Правильное создание составных ID состояний (`o12345s12345`)
- **Файл**: `graph-manager.js`

### **2. Ошибки валидации LLM**
- **Было**: "Действие не содержит ключа: action_links"
- **Исправлено**: `action_links` и `object_links` обязательны, но могут быть пустыми
- **Файл**: `api.py`

### **3. Таймауты Ollama**
- **Было**: Таймаут 30 секунд
- **Исправлено**: Таймаут 120 секунд
- **Файл**: `api.py`

## 🚀 Быстрый старт

### **1. Запуск сервера:**
```bash
python api.py
```

### **2. Открыть в браузере:**
Откройте `proxy-index.html` в браузере.

### **3. Тестирование:**
```bash
# Проверка валидации
python3 test_validation.py

# Проверка в браузере
open test-fix.html
```

## 📁 Ключевые файлы

### **`api.py`** - AI API сервер
- Генерация моделей с помощью LLM (Ollama/DeepSeek)
- Валидация структуры моделей
- Поддержка fallback моделей

### **`graph-manager.js`** - Логика работы с графами
- Обработка моделей от API
- Создание узлов и связей
- Автоматическое создание отсутствующих узлов
- Детальное логирование

### **`script.js`** - Визуализация графов
- Рендеринг графов с помощью Cytoscape.js
- Интерфейс пользователя

## 🧪 Тестирование

### **Доступные тесты:**
1. **`test_validation.py`** - проверка правил валидации
2. **`test-fix.html`** - проверка логики в браузере

### **Проверка исправлений:**
1. Запустите API сервер
2. Откройте Graph Editor в браузере
3. Проверьте генерацию модели
4. Убедитесь, что нет ошибок в консоли

## 📚 Подробная документация

Полная документация по исправлениям и правилам валидации в файле:
**[FINAL_IMPLEMENTATION_SUMMARY.md](FINAL_IMPLEMENTATION_SUMMARY.md)**

## 🔧 Технические детали

### **Правила валидации модели:**
- `action_links` и `object_links` обязательны
- Должны содержать: `manual`, `API`, `UI`
- Значения могут быть пустыми строками `""`

### **Форматы ID:**
- `object_id`: `"o" + 5 цифр` (пример: `"o12345"`)
- `state_id`: `"s" + 5 цифр` (пример: `"s12345"`)
- **Составной ID состояния**: `object_id + state_id` (пример: `"o12345s12345"`)

## 📞 Поддержка

Для отладки используйте:
1. Консоль браузера (F12 → Console)
2. Логи API сервера
3. Детальное логирование в `graph-manager.js`

---

**Все исправления обратно совместимы с существующим кодом.**