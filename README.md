# 🎨 Graph Editor - Визуальный редактор системных моделей

## 🚀 Быстрый старт

```bash
# 1. Дать разрешение на выполнение
chmod +x launch.command

# 2. Запустить систему
./launch.command
```

## 📋 Что такое Graph Editor?

**Graph Editor** - это инструмент для визуального моделирования систем на основе технических заданий (ТЗ).

### Как это работает:
1. **Загружаете ТЗ** (текстовый файл или вводите текст)
2. **Система анализирует** текст и находит:
   - Основные действия
   - Объекты и их состояния
   - Связи между элементами
3. **Генерирует JSON модель** в стандартизированном формате
4. **Визуализирует граф** с правилами отрисовки:
   - Действия → прямоугольники
   - Объекты + состояния → овалы
   - Связи → стрелки

## 🏗️ Архитектура

### Основные компоненты:

#### 1. **AI API сервер** (`api_simple_with_logging.py`)
- Принимает текстовые запросы
- Генерирует JSON модель
- **Выводит полный JSON в логи** с форматированием
- Статистика: действия, объекты, связи

#### 2. **Прокси сервер** (`proxy-server.js`)
- Маршрутизирует запросы
- Обслуживает статические файлы
- Решает проблему CORS

#### 3. **Веб-интерфейс**
- `proxy-index.html` - главная страница
- `script.js` - логика интерфейса
- `graph-manager.js` - управление графом
- `styles.css` - оформление

## 🎯 Ключевые особенности

### ✅ Автоматический анализ ТЗ
- Находит основное действие
- Определяет начальные и конечные условия
- Выявляет объекты и их состояния

### ✅ Стандартизированный JSON формат
```json
{
  "model_actions": [...],     // Действия
  "model_objects": [...],     // Объекты с состояниями  
  "model_connections": [...]  // Связи
}
```

### ✅ Визуализация с правилами
- **Действия**: прямоугольники
- **Состояния**: овалы
- **Связи**: стрелки (connection_out → connection_in)

### ✅ Полное логирование
- JSON выводится в консоль после каждой генерации
- Форматирование: `indent=2`, `ensure_ascii=False`
- Статистика модели

## 🔧 Технические требования

### Необходимое ПО:
- **Node.js** (v14+) - для прокси сервера
- **Python 3** (v3.8+) - для AI API
- **Браузер** (Chrome, Firefox, Safari)

### Порты:
- **API сервер**: 5001-5020 (автовыбор)
- **Прокси**: 3000
- **Веб-интерфейс**: http://localhost:3000/proxy-index.html

## 📚 Примеры

### Пример ТЗ (`exam.txt`):
```
Пользователь регистрируется в системе, вводя email и пароль.
Система проверяет email и создает учетную запись.
```

### Генерируемая модель:
```json
{
  "model_actions": [
    {
      "action_id": "a00001",
      "action_name": "Регистрация пользователя",
      "action_links": {"manual": "", "API": "", "UI": ""}
    }
  ],
  "model_objects": [
    {
      "object_id": "o00001",
      "object_name": "Пользователь",
      "resource_state": [
        {"state_id": "s00001", "state_name": "незарегистрирован"},
        {"state_id": "s00002", "state_name": "зарегистрирован"}
      ],
      "object_links": {"manual": "", "API": "", "UI": ""}
    }
  ],
  "model_connections": [
    {"connection_out": "o00001s00001", "connection_in": "a00001"},
    {"connection_out": "a00001", "connection_in": "o00001s00002"}
  ]
}
```

## 🐛 Устранение неисправностей

### Проблема: API не запускается
**Решение:**
```bash
# Проверить порты
lsof -i :5001-5020

# Остановить процессы
pkill -f "python.*api"
pkill -f "node proxy-server"

# Запустить вручную
python3 api_simple_with_logging.py
node proxy-server.js
```

### Проблема: JSON не выводится в логи
**Решение:** Убедиться, что запущен `api_simple_with_logging.py`

### Проблема: CORS ошибки
**Решение:** Использовать прокси (порт 3000)

## 📁 Структура проекта

```
.
├── launch.command              # Основной скрипт запуска
├── api_simple_with_logging.py  # AI API сервер
├── proxy-server.js            # Прокси сервер
├── proxy-index.html           # Веб-интерфейс
├── script.js                  # Клиентский скрипт
├── graph-manager.js           # Управление графом
├── styles.css                 # Стили
├── exam.txt                   # Пример ТЗ
├── DOCUMENTATION.md           # Полная документация
├── CHANGELOG.md               # История изменений
└── requirements.txt           # Зависимости Python
```

## 📞 Поддержка

### Полезные команды:
```bash
# Запуск только API
python3 api_simple_with_logging.py

# Запуск только прокси
node proxy-server.js

# Проверка работы
curl http://localhost:[PORT]/api/health

# Очистка логов
rm -f api.log proxy.log
```

### Логи:
- **Консоль терминала** - вывод API сервера
- **Консоль браузера (F12)** - клиентские логи
- **Файлы**: `api.log`, `proxy.log`

---

**🚀 Готово к использованию! Запустите `./launch.command` и начните создавать модели.**

📚 **Полная документация:** [DOCUMENTATION.md](DOCUMENTATION.md)
