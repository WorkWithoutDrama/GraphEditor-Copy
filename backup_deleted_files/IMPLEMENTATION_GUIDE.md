
📋 РУКОВОДСТВО ПО РЕАЛИЗАЦИИ СИСТЕМЫ

1. ✅ ПРОМПТ В API (api-fixed-new-structure.py):
   - Находит одно основное действие в описании
   - Определяет начальные и конечные условия
   - Добавляет новые элементы в модель
   - Генерирует JSON с правильной структурой

2. ✅ СТРУКТУРА JSON:
   {
     "model_actions": [    // Действия в прямоугольниках
       {
         "action_id": "a12345",
         "action_name": "Название",
         "action_links": {"manual": "", "API": "", "UI": ""}
       }
     ],
     "model_objects": [    // Объекты+состояния в овалах
       {
         "object_id": "o12345",
         "object_name": "Объект",
         "resource_state": [
           {"state_id": "s12345", "state_name": "Состояние"}
         ],
         "object_links": {"manual": "", "API": "", "UI": ""}
       }
     ],
     "model_connections": [    // Стрелки между узлами
       {
         "connection_out": "источник",
         "connection_in": "цель"  // формат: o12345s12345
       }
     ]
   }

3. ✅ ОТРИСОВКА ГРАФА:
   - Действия → Прямоугольники (script.js: node[type="action"])
   - Объект+состояние → Овалы (script.js: node[type="state"])
   - Стрелки → connection_in (начало) → connection_out (конец)
   - Обработка в graph-manager.js: processGraphResponse()

4. ✅ ДОБАВЛЕНИЕ НОВЫХ ЭЛЕМЕНТОВ:
   - Если действия нет в model_actions → добавить
   - Если объекта нет в model_objects → добавить
   - Если состояния нет в resource_state → добавить
   - Обновить model_connections

5. ✅ ПРОВЕРКИ:
   - Тестирование: test_new_prompt.py
   - Валидация структуры: test_validation.py
   - Пример: sample_model_correct.json

СИСТЕМА ГОТОВА К ИСПОЛЬЗОВАНИЮ! 🎯
