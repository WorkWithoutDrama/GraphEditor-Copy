# 📋 ДОКУМЕНТАЦИЯ Graph Editor

## 🚀 БЫСТРЫЙ СТАРТ

### 1. Запуск системы
```bash
chmod +x launch.command
./launch.command
```

### 2. Что запускается:
- **AI API сервер** (`api_simple_with_logging.py`) - порт 5001+
- **Прокси сервер** (`proxy-server.js`) - порт 3000
- **Веб-интерфейс** - автоматически открывается в браузере

### 3. Проверка работы:
```bash
# Проверить API
curl http://localhost:[PORT]/api/health

# Проверить прокси
curl http://localhost:3000
```

## 🏗️ АРХИТЕКТУРА

### Файловая структура:
```
.
├── launch.command              # Основной скрипт запуска
├── api_simple_with_logging.py  # AI API сервер с выводом JSON
├── proxy-server.js            # Прокси сервер
├── proxy-index.html           # Главная страница
├── script.js                  # Клиентский скрипт
├── graph-manager.js           # Управление графом
├── styles.css                 # Стили
├── exam.txt                   # Пример технического задания
├── DOCUMENTATION.md           # Эта документация
└── .gitignore                 # Игнорируемые файлы
```

### Компоненты:
1. **AI API сервер** (`api_simple_with_logging.py`):
   - Принимает текстовые запросы
   - Генерирует JSON модель
   - Выводит полный JSON в логи
   - Статистика: действия, объекты, связи

2. **Прокси сервер** (`proxy-server.js`):
   - Маршрутизирует запросы
   - Обслуживает статические файлы
   - Решает проблему CORS

3. **Клиентская часть**:
   - `proxy-index.html` - интерфейс редактора
   - `script.js` - логика интерфейса
   - `graph-manager.js` - управление графом
   - `styles.css` - оформление

## 🎯 ФУНКЦИОНАЛЬНОСТЬ

### Генерация модели из ТЗ:
1. **Загрузка файла** (.txt, .md, .pdf) или **ввод текста**
2. **Анализ текста** - нахождение:
   - Основного действия
   - Начальных условий (объекты + состояния)
   - Конечных условий (объекты + состояния)
3. **Генерация JSON модели**:
   ```json
   {
     "model_actions": [...],     // Действия (прямоугольники)
     "model_objects": [...],     // Объекты с состояниями (овалы)
     "model_connections": [...]  // Связи между узлами
   }
   ```
4. **Отрисовка графа**:
   - Действия → прямоугольники
   - Объекты + состояния → овалы
   - Связи → стрелки

### Правила отрисовки:
- **Действия**: `node[type="action"]` → прямоугольники
- **Состояния**: `node[type="state"]` → овалы
- **Связи**: `connection_out` → `connection_in`

### Идентификаторы:
- `action_id`: "a" + 5 цифр (пример: "a00001")
- `object_id`: "o" + 5 цифр (пример: "o00001")
- `state_id`: "s" + 5 цифр (пример: "s00001")
- **Составной ID**: `object_id + state_id` (пример: "o00001s00001")

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### API эндпоинты:
- `GET /api/health` - проверка доступности
- `POST /api/generate-model` - генерация модели
  ```json
  {
    "text": "Текст технического задания"
  }
  ```

### Логирование:
API выводит в консоль:
```
📥 ПОЛУЧЕН ЗАПРОС:
• Текст: [текст запроса]
🔄 ГЕНЕРАЦИЯ МОДЕЛИ...
🎯 СГЕНЕРИРОВАННАЯ МОДЕЛЬ:
[ПОЛНЫЙ JSON С ФОРМАТИРОВАНИЕМ]
📊 СТАТИСТИКА МОДЕЛИ:
• Действий: X, Объектов: Y, Связей: Z
✅ ОТВЕТ ОТПРАВЛЕН
```

### Формат JSON модели:
```json
{
  "model_actions": [
    {
      "action_id": "a12345",
      "action_name": "Название действия",
      "action_links": {"manual": "", "API": "", "UI": ""}
    }
  ],
  "model_objects": [
    {
      "object_id": "o12345",
      "object_name": "Название объекта",
      "resource_state": [
        {"state_id": "s00000", "state_name": "null"},
        {"state_id": "s12345", "state_name": "состояние объекта"}
      ],
      "object_links": {"manual": "", "API": "", "UI": ""}
    }
  ],
  "model_connections": [
    {
      "connection_out": "идентификатор_источника",
      "connection_in": "идентификатор_цели"
    }
  ]
}
```

## 🐛 УСТРАНЕНИЕ НЕИСПРАВНОСТЕЙ

### Проблема: API не запускается
**Решение:**
```bash
# Проверить порты
lsof -i :5001-5020

# Остановить все процессы
pkill -f "python.*api"
pkill -f "node proxy-server"

# Запустить вручную
python3 api_simple_with_logging.py
node proxy-server.js
```

### Проблема: JSON не выводится в логи
**Решение:** Убедиться, что запущен `api_simple_with_logging.py` (выводит JSON)

### Проблема: CORS ошибки
**Решение:** Использовать прокси (порт 3000)

### Проблема: Граф не отрисовывается
**Решение:** Проверить консоль браузера (F12)

## 📚 ПРИМЕРЫ

### Пример ТЗ (`exam.txt`):
```
ТЕХНИЧЕСКОЕ ЗАДАНИЕ
Проект: Система регистрации пользователей
Описание: Пользователь регистрируется в системе, вводя email и пароль. 
Система проверяет email и создает учетную запись.
```

### Пример JSON модели:
```json
{
  "model_actions": [
    {
      "action_id": "a00001",
      "action_name": "Регистрация пользователя",
      "action_links": {"manual": "", "API": "", "UI": ""}
    }
  ],
  "model_objects": [
    {
      "object_id": "o00001",
      "object_name": "Пользователь",
      "resource_state": [
        {"state_id": "s00001", "state_name": "незарегистрирован"},
        {"state_id": "s00002", "state_name": "зарегистрирован"}
      ],
      "object_links": {"manual": "", "API": "", "UI": ""}
    },
    {
      "object_id": "o00002",
      "object_name": "Система",
      "resource_state": [
        {"state_id": "s00003", "state_name": "ожидает регистрации"},
        {"state_id": "s00004", "state_name": "пользователь зарегистрирован"}
      ],
      "object_links": {"manual": "", "API": "", "UI": ""}
    }
  ],
  "model_connections": [
    {
      "connection_out": "o00001s00001",
      "connection_in": "a00001"
    },
    {
      "connection_out": "a00001",
      "connection_in": "o00001s00002"
    },
    {
      "connection_out": "a00001",
      "connection_in": "o00002s00004"
    }
  ]
}
```

## 📞 ПОДДЕРЖКА

### Полезные команды:
```bash
# Запуск только API
python3 api_simple_with_logging.py

# Запуск только прокси
node proxy-server.js

# Проверка портов
curl http://localhost:[PORT]/api/health

# Очистка логов
rm -f api.log proxy.log
```

### Логи:
- `api.log` - логи API сервера
- Консоль браузера (F12) - клиентские логи
- Консоль терминала - серверные логи

---

**✅ Система готова к работе! Запустите `./launch.command` и начните создавать модели.**