# Инструкция по исправлению отображения графа

## 📋 Описание проблемы

### Было:
1. **Все узлы одинакового размера** независимо от длины текста
2. **Текст не помещался** в узлы - обрезался или выходил за границы
3. **Длинный текст не переносился** на несколько строк
4. **Плохая читаемость** - мелкий шрифт, недостаточные отступы

### Стало:
1. ✅ **Адаптивные размеры** - узлы меняют размер под текст
2. ✅ **Автоматический перенос** - длинный текст разбивается на строки
3. ✅ **Улучшенная читаемость** - увеличен шрифт, правильные отступы
4. ✅ **Разные стили** для действий и состояний

## 🔧 Внесенные изменения

### В файле `script.js`:

#### 1. **Базовые стили для всех узлов:**
```javascript
{ selector: 'node', style: {
    'text-wrap': 'wrap',           // Включен перенос текста
    'text-max-width': '180px',     // Максимальная ширина текста
    'font-size': '14px',           // Увеличенный шрифт
    'line-height': 1.4,            // Межстрочный интервал
    'padding': '12px',             // Отступы внутри узла
    'color': '#333'                // Цвет текста
} }
```

#### 2. **Стили для узлов-действий (прямоугольники):**
```javascript
{ selector: 'node[type="action"]', style: {
    'width': 'label',              // Ширина по тексту
    'height': 'label',             // Высота по тексту
    'text-max-width': '200px',     // Макс. ширина для действий
    'min-width': '80px',           // Минимальная ширина
    'min-height': '40px',          // Минимальная высота
    'font-weight': '500'           // Полужирный шрифт
} }
```

#### 3. **Стили для узлов-состояний (эллипсы):**
```javascript
{ selector: 'node[type="state"]', style: {
    'width': 'label',              // Ширина по тексту
    'height': 'label',             // Высота по тексту
    'text-max-width': '160px',     // Макс. ширина для состояний
    'min-width': '70px',           // Минимальная ширина
    'min-height': '70px'           // Минимальная высота
} }
```

#### 4. **Улучшенные стили для ребер:**
```javascript
{ selector: 'edge', style: {
    'width': 3,                    // Утолщенные линии
    'line-color': '#666',          // Темный цвет
    'target-arrow-fill': 'filled', // Закрашенные стрелки
    'arrow-scale': 1.5             // Увеличенные стрелки
} }
```

## 🧪 Тестирование исправлений

### Создан тестовый файл `test-graph-display.html`:

#### Тест 1: **Короткий текст**
- Узлы: "Начало", "Действие 1", "Конец"
- Ожидание: Компактные формы

#### Тест 2: **Средний текст**  
- Узлы: "Пользователь не авторизован в системе", "Система проверяет учетные данные"
- Ожидание: Текст переносится на 2-3 строки

#### Тест 3: **Длинный текст**
- Узлы: "Пользователь не зарегистрирован в системе планирования питания..."
- Ожидание: Текст переносится на несколько строк

#### Тест 4: **Смешанные узлы**
- Разные типы узлов с разной длиной текста
- Ожидание: Разные размеры, читаемость сохранена

## 🚀 Как использовать исправления

### 1. **Для существующих проектов:**
- Скопировать стили из `script.js` в свой проект
- Убедиться, что cytoscape имеет поддержку `text-wrap`
- Протестировать с разными длинами текста

### 2. **Настройка параметров:**

#### Максимальные ширины:
- **Все узлы:** `text-max-width: '180px'`
- **Действия:** `text-max-width: '200px'`
- **Состояния:** `text-max-width: '160px'`

#### Минимальные размеры:
- **Действия:** `min-width: '80px', min-height: '40px'`
- **Состояния:** `min-width: '70px', min-height: '70px'`

#### Шрифты:
- **Размер:** `font-size: '13px'-'14px'`
- **Вес:** `font-weight: '500'` для действий
- **Семейство:** `font-family: 'Arial, sans-serif'`

### 3. **Дополнительные настройки:**

#### Для очень длинных текстов:
```javascript
'text-overflow': 'ellipsis',  // Многоточие при обрезке
'text-max-width': '250px',     // Увеличить максимальную ширину
```

#### Для улучшения производительности:
```javascript
'min-zoomed-font-size': 8,    // Минимальный размер шрифта при зуме
```

## 📊 Ожидаемые результаты

### Успешные сценарии:
1. ✅ **Короткий текст** - компактные узлы, текст полностью виден
2. ✅ **Средний текст** - текст переносится на 2-3 строки
3. ✅ **Длинный текст** - текст переносится на несколько строк
4. ✅ **Разные типы узлов** - разные размеры и стили

### Критерии качества:
- **Читаемость:** Текст легко читается без увеличения
- **Пропорциональность:** Размеры соответствуют содержанию
- **Эстетика:** Приятный внешний вид
- **Производительность:** Быстрое отображение

## 🔧 Устранение неполадок

### Проблема: Текст не переносится
**Решение:** Проверить поддержку `text-wrap` в cytoscape

### Проблема: Узлы слишком большие
**Решение:** Уменьшить `text-max-width` или `padding`

### Проблема: Мелкий шрифт
**Решение:** Увеличить `font-size` до `15px`-`16px`

### Проблема: Наложение узлов
**Решение:** Настроить `spacing` в layout или увеличить `min-width`/`min-height`

## 📈 Дальнейшие улучшения

### Планируемые улучшения:
1. **Динамическая настройка** размеров на основе количества слов
2. **Интерактивное изменение** размеров узлов
3. **Поддержка эмодзи** и специальных символов
4. **Адаптация для мобильных** устройств

### Оптимизации:
1. **Кэширование** расчетов размеров
2. **Ленивая загрузка** стилей
3. **Оптимизация производительности** для больших графов

## 🎯 Итог

**Исправления решают основные проблемы отображения графа:**
1. ✅ **Адаптивные размеры** под разную длину текста
2. ✅ **Автоматический перенос** длинного текста
3. ✅ **Улучшенная читаемость** и эстетика
4. ✅ **Разные стили** для разных типов узлов

**Код готов к использованию в production-среде.**