# 🎯 Усовершенствование преобразования ТЗ в граф

## 📋 Задача
Усовершенствовать существующую систему генерации графов из технических заданий:
1. Добавить запрос имени модели у пользователя перед генерацией
2. Сохранять JSON модель в папку `models/`
3. Улучшить пользовательский интерфейс

## ✅ Выполненные изменения

### 1. **Обновлен `graph-manager.js`:**
- Добавлен метод `promptForModelName()` для запроса имени модели у пользователя
- Модифицирован метод `generateModelFromText(text, modelName)` для передачи имени модели в API
- Обновлены обработчики текстовых запросов и загрузки файлов для запроса имени модели
- Добавлено логирование имени модели в консоль

### 2. **Обновлен `api_main.py`:**
- Добавлен метод `save_model_to_file(model, model_name)` для сохранения моделей в папку `models/`
- Метод создает папку `models/` если она не существует
- Сохраняет модели в формате JSON с метаданными:
  - Имя модели
  - Время генерации
  - Источник генерации
  - Структура модели (действия, объекты, связи)
- Добавлено получение имени модели из POST запроса
- Модель сохраняется перед отправкой ответа клиенту

### 3. **Создана папка `models/`:**
- Папка для хранения всех сгенерированных JSON моделей
- Каждая модель сохраняется как `models/[имя_модели].json`
- Автоматическое создание папки при первом сохранении

### 4. **Добавлен тестовый файл:**
- `test_tz.txt` - пример технического задания для тестирования системы

## 🔧 Технические детали

### **Новый поток работы:**
1. Пользователь вводит ТЗ или загружает файл
2. Система запрашивает имя для модели через `prompt()`
3. Если имя не указано - процесс отменяется
4. Запрос отправляется в API с именем модели
5. API генерирует модель и сохраняет ее в `models/[имя_модели].json`
6. Модель отображается в веб-интерфейсе
7. Пользователь получает подтверждение с именем сохраненной модели

### **Формат сохраненной модели:**
```json
{
  "version": "1.0",
  "metadata": {
    "name": "имя_модели",
    "generated_at": "2024-02-11T14:00:00",
    "source": "api_main.py"
  },
  "model_actions": [...],
  "model_objects": [...],
  "model_connections": [...]
}
```

## 🚀 Как тестировать

### **1. Запустить систему:**
```bash
chmod +x launch.command
./launch.command
```

### **2. Протестировать через веб-интерфейс:**
1. Открыть `http://localhost:3000/proxy-index.html`
2. Нажать кнопку "Graph Manager"
3. Ввести текст ТЗ или загрузить `test_tz.txt`
4. Ввести имя модели при запросе
5. Проверить сохранение в папке `models/`

### **3. Проверить работу API напрямую:**
```bash
python3 api_main.py &
sleep 2
curl -X POST http://localhost:5009/api/generate-model \
  -H "Content-Type: application/json" \
  -d '{"text": "Тестовое ТЗ", "model_name": "test_model"}'
```

## 📁 Структура проекта после изменений
```
.
├── launch.command          # Основной скрипт запуска
├── api_main.py             # Обновленный API сервер
├── proxy-server.js         # Прокси сервер
├── proxy-index.html        # Веб-интерфейс
├── graph-manager.js        # Обновленный Graph Manager
├── script_fixed.js         # Клиентская логика
├── styles.css              # Стили
├── models/                 # Новая папка для моделей
│   └── README.md           # Описание папки models
├── api_simple_with_cors.py # Резервный API
├── requirements.txt        # Зависимости
├── README.md               # Основная документация
└── .gitignore              # Игнорируемые файлы
```

## 🎯 Результат
Система теперь запрашивает имя модели у пользователя и автоматически сохраняет сгенерированные JSON модели в организованную папку `models/`. Это позволяет:
- Легко находить и повторно использовать ранее созданные модели
- Организовать работу с несколькими моделями
- Следить за историей генерации моделей
- Улучшить пользовательский опыт