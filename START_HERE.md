# Graph Editor - Быстрый старт 🚀

## 🎯 ОДНА КОМАНДА ДЛЯ ЗАПУСКА:

### Способ 1: Просто дважды кликните:
**🖱️ ДВАЖДЫ КЛИКНИТЕ на `start.command`**

### Способ 2: Через терминал:
```bash
./start.command
```

## Что произойдет:
1. ✅ **Запустится AI сервер** (порт 5000)
2. ✅ **Запустится прокси** (порт 3000)
3. ✅ **Откроется браузер** с редактором
4. ✅ **Можно использовать** Graph Manager

## 🔧 Если не работает:

### Проверьте зависимости:
```bash
./check-deps.command
```

### Установите если нужно:
- **Node.js**: https://nodejs.org/
- **Python 3**: https://www.python.org/

### Разрешите выполнение:
```bash
chmod +x start.command
```

## 📱 После запуска:
1. Нажмите **"Graph Manager"** в редакторе
2. Отправьте **описание системы**
3. **AI создаст графовую модель**

## 🛑 Для остановки:
Нажмите **Ctrl+C** в терминале

---

**Готово! Система работает в полном режиме с AI.** 🎉

## Что делает скрипт:

1. **Запускает AI API сервер** (порт 5000)
2. **Запускает прокси сервер** (порт 3000)  
3. **Открывает браузер** с Graph Editor
4. **Показывает статус** в терминале

## После запуска:

1. **Нажмите кнопку** "Graph Manager" в редакторе
2. **Отправьте** описание системы
3. **AI создаст** графовую модель

## Для остановки:
Нажмите **Ctrl+C** в терминале где работает скрипт

## Проблемы?

### ❌ "Файл поврежден"
```bash
# В терминале:
sudo xattr -rd com.apple.quarantine start.command
```

### ❌ "Команда не найдена"
Установите:
- **Node.js**: https://nodejs.org/
- **Python 3**: https://www.python.org/

### ❌ "Порт занят"
Закройте другие копии скрипта или:
```bash
# Остановите все:
pkill -f "python api.py"
pkill -f "node proxy-server.js"
```

## Готово! 🎉