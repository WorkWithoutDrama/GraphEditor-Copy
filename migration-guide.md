# Инструкция по миграции на новую структуру JSON

## 📋 Обзор изменений

### Старая структура:
```json
{
  "Действие": {
    "init_states": ["Состояние 1"],
    "final_states": ["Состояние 2"]
  }
}
```

### Новая структура:
```json
{
  "model_actions": [
    {
      "action_id": "a1234567890123",
      "action_name": "Действие",
      "action_links": {}
    }
  ],
  "model_objects": [
    {
      "object_id": "o1234567890123",
      "object_name": "Объект",
      "resource_state": {
        "state_id": "s1234567890123",
        "state_name": "Состояние 1"
      },
      "object_links": {}
    },
    {
      "object_id": "o1234567890124",
      "object_name": "Объект",
      "resource_state": {
        "state_id": "s1234567890124",
        "state_name": "Состояние 2"
      },
      "object_links": {}
    }
  ],
  "project_connections": [
    {
      "connection_id": "c1234567890123",
      "connection_out": "o1234567890123s1234567890123",
      "connection_in": "a1234567890123",
      "connection_type": "initiates"
    },
    {
      "connection_id": "c1234567890124",
      "connection_out": "a1234567890123",
      "connection_in": "o1234567890124s1234567890124",
      "connection_type": "results_in"
    }
  ]
}
```

## 🔄 Алгоритм миграции

### Шаг 1: Извлечение действий
Для каждого ключа в старой структуре:
1. Создать `action_id` по шаблону: `a{timestamp}{random}`
2. Сохранить `action_name` из ключа
3. Добавить в `model_actions`

### Шаг 2: Извлечение состояний
Для каждого состояния в `init_states` и `final_states`:
1. Определить объект из состояния (например, "Пользователь" из "Пользователь зарегистрирован")
2. Создать `object_id`: `o{timestamp}{random}`
3. Создать `state_id`: `s{timestamp}{random}`
4. Извлечить `state_name` из состояния
5. Добавить в `model_objects`

### Шаг 3: Создание связей
Для каждой пары действие-состояние:
1. Создать `connection_id`: `c{timestamp}{random}`
2. Для `init_states`: `connection_type = "initiates"`
3. Для `final_states`: `connection_type = "results_in"`
4. `connection_out` = `object_id` + `state_id`
5. `connection_in` = `action_id`
6. Добавить в `project_connections`

## 🧰 Утилиты для миграции

### Функция миграции (JavaScript):
```javascript
function migrateOldToNew(oldModel) {
  const newModel = {
    model_actions: [],
    model_objects: [],
    project_connections: []
  };

  const usedObjects = {}; // object_name -> {object_id, states: {}}
  let counter = 1;

  // Генерация ID
  function generateId(prefix) {
    return prefix + Date.now() + counter++;
  }

  // Извлечение объекта из состояния
  function extractObjectFromState(state) {
    // Логика извлечения объекта из состояния
    // Пример: "Пользователь зарегистрирован" -> "Пользователь"
    if (state.includes(' ')) {
      return state.split(' ')[0];
    }
    return state;
  }

  // Обработка старой модели
  for (const [actionName, actionData] of Object.entries(oldModel)) {
    // 1. Создать действие
    const actionId = generateId('a');
    newModel.model_actions.push({
      action_id: actionId,
      action_name: actionName,
      action_links: {}
    });

    // 2. Обработать init_states
    if (actionData.init_states) {
      actionData.init_states.forEach(state => {
        const objectName = extractObjectFromState(state);
        const stateId = generateId('s');
        
        // Проверить, есть ли уже такой объект
        if (!usedObjects[objectName]) {
          const objectId = generateId('o');
          usedObjects[objectName] = {
            object_id: objectId,
            states: {}
          };
          
          newModel.model_objects.push({
            object_id: objectId,
            object_name: objectName,
            resource_state: {
              state_id: stateId,
              state_name: state
            },
            object_links: {}
          });
        }
        
        // Запомнить состояние
        usedObjects[objectName].states[state] = stateId;
        
        // Создать связь
        newModel.project_connections.push({
          connection_id: generateId('c'),
          connection_out: `${usedObjects[objectName].object_id}${stateId}`,
          connection_in: actionId,
          connection_type: "initiates"
        });
      });
    }

    // 3. Обработать final_states (аналогично)
    // ...
  }

  return newModel;
}
```

### Функция миграции (Python):
```python
def migrate_old_to_new(old_model):
    import time
    import random
    
    new_model = {
        "model_actions": [],
        "model_objects": [],
        "project_connections": []
    }
    
    used_objects = {}
    counter = 1
    
    def generate_id(prefix):
        nonlocal counter
        id_str = f"{prefix}{int(time.time() * 1000)}{counter:04d}"
        counter += 1
        return id_str
    
    def extract_object_from_state(state):
        if " " in state:
            return state.split(" ")[0]
        return state
    
    for action_name, action_data in old_model.items():
        # Создать действие
        action_id = generate_id("a")
        new_model["model_actions"].append({
            "action_id": action_id,
            "action_name": action_name,
            "action_links": {}
        })
        
        # Обработать init_states
        if "init_states" in action_data:
            for state in action_data["init_states"]:
                object_name = extract_object_from_state(state)
                state_id = generate_id("s")
                
                if object_name not in used_objects:
                    object_id = generate_id("o")
                    used_objects[object_name] = {
                        "object_id": object_id,
                        "states": {}
                    }
                    
                    new_model["model_objects"].append({
                        "object_id": object_id,
                        "object_name": object_name,
                        "resource_state": {
                            "state_id": state_id,
                            "state_name": state
                        },
                        "object_links": {}
                    })
                
                used_objects[object_name]["states"][state] = state_id
                
                new_model["project_connections"].append({
                    "connection_id": generate_id("c"),
                    "connection_out": f"{used_objects[object_name]['object_id']}{state_id}",
                    "connection_in": action_id,
                    "connection_type": "initiates"
                })
        
        # Обработать final_states аналогично
        # ...
    
    return new_model
```

## 📁 Файлы для обновления

### 1. **api.py**:
- Обновить `LLMClient.generate_model()` для новой структуры
- Обновить промпты
- Добавить валидацию новой структуры

### 2. **graph-manager.js**:
- Обновить `processGraphResponse()`
- Реализовать преобразование новой структуры в граф
- Обновить логику отображения

### 3. **script.js**:
- Обновить стили Cytoscape
- Реализовать отображение составных ID
- Обновить логику редактирования

### 4. **Создать миграционные утилиты**:
- `migrate-old-to-new.js` - конвертер
- `validate-new-structure.js` - валидатор
- `example-new-model.json` - пример

## 🧪 Тестирование миграции

### Тест 1: Простая миграция
```javascript
const oldModel = {
  "Пользователь регистрируется": {
    "init_states": ["Нет сессии"],
    "final_states": ["Пользователь зарегистрирован"]
  }
};

const newModel = migrateOldToNew(oldModel);
// Проверить:
// 1. Есть ли action "Пользователь регистрируется"
// 2. Есть ли объект "Пользователь" с двумя состояниями
// 3. Есть ли правильные связи
```

### Тест 2: Миграция с несколькими действиями
```javascript
const oldModel = {
  "Регистрация": {
    "init_states": ["Нет сессии"],
    "final_states": ["Пользователь зарегистрирован"]
  },
  "Авторизация": {
    "init_states": ["Пользователь зарегистрирован"],
    "final_states": ["Пользователь авторизован"]
  }
};
// Проверить правильность связей между действиями
```

### Тест 3: Миграция с дублирующимися состояниями
```javascript
const oldModel = {
  "Действие 1": {
    "init_states": ["Состояние А"],
    "final_states": ["Состояние Б"]
  },
  "Действие 2": {
    "init_states": ["Состояние Б"],
    "final_states": ["Состояние В"]
  }
};
// Проверить, что "Состояние Б" используется один раз
```

## 🚀 План внедрения

### Фаза 1: Подготовка (1 день)
1. Создать утилиты миграции
2. Создать примеры новой структуры
3. Обновить документацию

### Фаза 2: Обновление промптов (1 день)
1. Обновить промпты в `api.py`
2. Протестировать генерацию новой структуры
3. Настроить валидацию

### Фаза 3: Обновление клиента (2 дня)
1. Обновить `graph-manager.js`
2. Обновить `script.js`
3. Протестировать отображение

### Фаза 4: Миграция данных (1 день)
1. Конвертировать существующие модели
2. Протестировать обратную совместимость
3. Создать fallback для старых моделей

### Фаза 5: Тестирование (1 день)
1. Полное тестирование миграции
2. Тестирование производительности
3. Сбор обратной связи

## ⚠️ Риски и mitigations

### Риск 1: Потеря данных при миграции
**Mitigation:** Сохранять старые модели, создавать backup

### Риск 2: Некорректное извлечение объектов из состояний
**Mitigation:** Ручная проверка, создание словарей маппинга

### Риск 3: Проблемы с производительностью
**Mitigation:** Инкрементальная миграция, кэширование

### Риск 4: Ошибки в новых промптах
**Mitigation:** A/B тестирование, постепенное внедрение

## 📊 Метрики успеха

### Технические:
1. ✅ 100% успешная миграция существующих моделей
2. ✅ Корректная генерация новой структуры LLM
3. ✅ Сохранение всех связей и семантики

### Бизнес:
1. ✅ Улучшенная читаемость моделей
2. ✅ Возможность расширения структуры
3. ✅ Подготовка к интеграции с БД

### Пользовательские:
1. ✅ Прозрачность миграции для пользователей
2. ✅ Сохранение работоспособности
3. ✅ Улучшенный UX с новой структурой

## 🎯 Итог

**Миграция на новую структуру JSON обеспечит:**
1. ✅ Чистое разделение действий, объектов и связей
2. ✅ Уникальные ID для всех элементов
3. ✅ Гибкость для будущих расширений
4. ✅ Подготовку к интеграции с базами данных
5. ✅ Улучшенную поддерживаемость кода

**Ветка готова к реализации!** 🚀