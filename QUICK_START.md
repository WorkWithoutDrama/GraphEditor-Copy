# 🚀 БЫСТРЫЙ СТАРТ: Система анализа ТЗ

## 📋 ЧТО СОЗДАНО

Создана полная система для автоматического анализа ТЗ и генерации моделей процессов на примере `exam.txt` (веб-приложение "Mindful Meals").

## 📁 ОСНОВНЫЕ ФАЙЛЫ

### 1. **Модели:**
- `mindful_meals_advanced.json` - Основная модель (18 действий, 20 объектов, 44 связи)

### 2. **Скрипты анализа:**
- `incremental_model_builder.py` - Построчное чтение ТЗ
- `advanced_model_builder.py` - Анализ с использованием шаблонов

### 3. **Визуализация:**
- `process_model.dot` - Граф для Graphviz
- `process_model_mermaid.md` - Mermaid.js граф  
- `model_viewer.html` - HTML просмотрщик

## ⚡ КАК ИСПОЛЬЗОВАТЬ

### 1. **Просмотр результата:**
```bash
# Откройте HTML просмотрщик
open model_viewer.html

# Или посмотрите Mermaid граф
cat process_model_mermaid.md
```

### 2. **Анализ нового ТЗ:**
```bash
# Создайте копию вашего ТЗ
cp ваш_тз.txt exam.txt

# Запустите анализатор
python3 advanced_model_builder.py
```

### 3. **Создание визуализации:**
```bash
# Установите Graphviz (если нет)
# brew install graphviz  # для macOS

# Создайте PNG из DOT
dot -Tpng process_model.dot -o model.png
```

## 🎯 КЛЮЧЕВЫЕ ВОЗМОЖНОСТИ

### ✅ **Автоматически определяет:**
- Действия (процессы) из ТЗ
- Объекты (сущности) системы
- Состояния объектов
- Связи между элементами

### ✅ **Исправляет ошибки LLM:**
- Некорректные состояния
- Отсутствующие связи
- Семантические несоответствия

### ✅ **Создает визуализации:**
- Graphviz для профессиональных диаграмм
- Mermaid для веб-документации
- HTML для интерактивного просмотра

## 🔧 НАСТРОЙКА

### Добавление новых шаблонов действий:
Отредактируйте `action_templates` в `advanced_model_builder.py`:
```python
action_templates = {
    "ваше_действие": {
        "required_objects": ["Объект1", "Объект2"],
        "input_states": {"Объект1": "начальное состояние"},
        "output_states": {"Объект2": "конечное состояние"}
    }
}
```

### Изменение паттернов поиска:
Настройте регулярные выражения для определения действий и объектов в ТЗ.

## 📊 ПРОВЕРКА РЕЗУЛЬТАТА

Откройте `model_viewer.html` в браузере:
1. Увидите **статистику** модели
2. **Визуализацию** процессов
3. **Таблицы** действий, объектов, связей

## 🚀 ДАЛЬНЕЙШЕЕ РАЗВИТИЕ

1. **Интеграция с LLM API** для улучшения анализа
2. **Веб-интерфейс** для загрузки ТЗ
3. **Экспорт в BPMN/UML** форматы
4. **Валидация бизнес-логики**

## 📞 ПОДДЕРЖКА

Система готова к использованию и расширению. Все исходные файлы содержат подробные комментарии.

---

**🎯 Итог:** Создана рабочая система, которая автоматически анализирует ТЗ, строит корректные модели процессов и создает multiple визуализации.